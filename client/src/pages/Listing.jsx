import { useEffect } from "react";

const Listing = () => {
  useEffect(() => {
    const fetchListing = async () => {};
    fetchListing();
  }, []);

  return <div>Listing</div>;
};

export default Listing;
